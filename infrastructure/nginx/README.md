# Nginx Configuration

This directory contains Nginx reverse proxy configuration.

## Purpose
- Reverse proxy configuration
- Load balancing setup
- SSL/TLS configuration
- Static file serving
- Caching rules

## Notes
- Nginx will serve as the entry point
- Routes requests to frontend and backend
- Supports horizontal scaling
