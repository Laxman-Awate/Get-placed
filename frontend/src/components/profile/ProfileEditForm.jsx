import React from 'react';
const fields=[['name','Full name'],['email','Email'],['phone','Phone'],['college','College'],['degree','Degree'],['branch','Branch'],['graduationYear','Graduation year'],['semester','Current semester'],['cgpa','CGPA'],['role','Preferred role'],['locations','Preferred locations']];
export function ProfileEditForm({profile,onChange,onSave,onCancel}){return <form className="profile-edit-form dashboard-panel" onSubmit={e=>{e.preventDefault();onSave()}}><div className="profile-edit-heading"><div><span className="section-kicker">EDIT PROFILE</span><h2>Keep your details current</h2></div><button className="subtle-button" type="button" onClick={onCancel}>Cancel</button></div><div className="profile-edit-grid">{fields.map(([key,label])=><label key={key}><span>{label}</span><input value={profile[key]||''} onChange={e=>onChange(key,e.target.value)} /></label>)}</div><label><span>Skills</span><input value={profile.skills.join(', ')} onChange={e=>onChange('skills',e.target.value.split(',').map(item=>item.trim()).filter(Boolean))}/></label><button className="button" type="submit">Save changes</button></form>}
