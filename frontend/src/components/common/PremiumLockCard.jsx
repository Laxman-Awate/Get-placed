import React from 'react'; import {PremiumBadge} from './PremiumBadge';
export function PremiumLockCard({title='Premium preparation',description='Unlock the complete preparation experience when you are ready.',features=[],href='/pricing',action='Explore premium'}){return <article className="premium-access-card"><div><PremiumBadge/><h3>{title}</h3><p>{description}</p>{features.length>0&&<ul>{features.map(feature=><li key={feature}>✓ {feature}</li>)}</ul>}</div><a className="button" href={href}>{action} <span>↗</span></a></article>}
