import React from 'react'; export function ReadinessBreakdown({metrics}){return <div className="readiness-breakdown">{metrics.map(metric=><article className="readiness-metric dashboard-panel" key={metric.key}><div><h3>{metric.label}</h3><strong>{metric.score}%</strong></div><i><em style={{width:`${metric.score}%`}} /></i><span className={metric.score>=75?'strong-label':'focus-label'}>{metric.score>=75?'Strong':metric.score<65?'Needs improvement':'Building'}</span><p>{metric.explanation}</p></article>)}</div>}
