import React from 'react';
export function AptitudeQuestionCard({ practice }) { const {question,selected,setSelected,submitted,submit,next,index,total}=practice; if(!question)return <div className="empty-companies"><h2>No practice questions available</h2><p>No questions are available for this topic yet.</p></div>; return <article className="aptitude-question dashboard-panel"><div className="question-top"><span>Question {index+1} of {total}</span><span className="difficulty easy">{question.difficulty}</span></div><h2>{question.question}</h2><div className="answer-options">{question.options.map((option,i)=><label className={submitted ? (i===question.correctAnswer?'correct':i===selected?'incorrect':'') : selected===i?'selected':''} key={option}><input type="radio" name="answer" checked={selected===i} onChange={()=>!submitted&&setSelected(i)} /><span>{String.fromCharCode(65+i)}</span>{option}</label>)}</div>{submitted&&<div className={selected===question.correctAnswer?'answer-feedback correct-feedback':'answer-feedback incorrect-feedback'}><strong>{selected===question.correctAnswer?'✓ Correct':'✕ Not quite'}</strong><p>{question.explanation}</p></div>}{!submitted?<button className="button" onClick={submit} disabled={selected===null}>Submit answer <span>↗</span></button>:<button className="button" onClick={next} disabled={index>=total-1}>Next question <span>→</span></button>}</article>; }
