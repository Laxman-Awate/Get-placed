import React from 'react'; import { FreeBadge } from '../common/FreeBadge'; import { PremiumBadge } from '../common/PremiumBadge';
import { Link } from '../../context/RouteContext';
export function AptitudeTopicCard({ topic, categoryId }) { return <article className="aptitude-topic-card"><div><h3>{topic.name}</h3><p>{topic.description}</p><span className="difficulty medium">{topic.difficulty}</span></div><div className="topic-card-meta"><span>{topic.questions} questions</span><b>{topic.progress}%</b></div>{topic.free ? <FreeBadge label="FREE PREVIEW"/> : <PremiumBadge label="LOCKED · PREMIUM"/>}<Link className={topic.free ? 'button' : 'outline-button'} href={topic.free ? `/practice/aptitude/${categoryId}/${topic.id}` : '/pricing'}>{topic.free ? 'Practice' : 'Unlock'} <span>↗</span></Link></article>; }
