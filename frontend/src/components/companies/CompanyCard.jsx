import React from 'react';
import { Link } from '../../context/RouteContext';
export function CompanyCard({ company, bookmarked, onBookmark }) { return <article className="company-card"><div className="company-card-top"><span className={`brand-mark brand-${company.id}`}>{company.name[0]}</span><button className={bookmarked ? 'company-bookmark active' : 'company-bookmark'} onClick={onBookmark} aria-label={bookmarked ? `Remove ${company.name} bookmark` : `Bookmark ${company.name}`}>{bookmarked ? '★' : '☆'}</button></div><div className="company-card-title"><h2>{company.name}</h2><span>{company.type}</span></div><p>{company.description}</p><small className="focus-label">PREPARATION FOCUS</small><div className="company-areas">{company.areas.map(area => <span key={area}>{area}</span>)}</div><div className="company-card-meta"><span>{company.modules} modules</span><b className={company.premium ? 'premium-label' : 'free-badge'}>{company.premium ? 'PREMIUM' : 'FREE'}</b></div><Link className="outline-button company-cta" href={`/companies/${company.id}`}>View preparation <span>↗</span></Link></article>; }
