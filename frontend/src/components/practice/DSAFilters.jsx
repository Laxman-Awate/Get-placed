import React from 'react';

export function DSAFilters({ filters, onChange }) {
  return <div className="dsa-filters"><label className="dsa-search"><span aria-hidden="true">⌕</span><input aria-label="Search DSA problems" value={filters.search} onChange={event => onChange({ search: event.target.value })} placeholder="Search DSA problems..." /></label><select aria-label="Filter by topic" value={filters.topic} onChange={event => onChange({ topic: event.target.value })}><option value="all">All topics</option><option value="arrays">Arrays</option><option value="strings">Strings</option><option value="linked-list">Linked List</option><option value="binary-search">Binary Search</option></select><select aria-label="Filter by difficulty" value={filters.difficulty} onChange={event => onChange({ difficulty: event.target.value })}><option value="all">All difficulty</option><option>Easy</option><option>Medium</option><option>Hard</option></select><select aria-label="Filter by status" value={filters.status} onChange={event => onChange({ status: event.target.value })}><option value="all">All status</option><option value="solved">Solved</option><option value="unsolved">Unsolved</option></select></div>;
}
