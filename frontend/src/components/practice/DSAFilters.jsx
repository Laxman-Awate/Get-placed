import React from 'react';
export function DSAFilters({ filters, onChange }) { return <div className="dsa-filters"><label className="dsa-search">⌕<input value={filters.search} onChange={event => onChange({ search: event.target.value })} placeholder="Search DSA problems..." /></label><select value={filters.topic} onChange={event => onChange({ topic: event.target.value })}><option value="all">All topics</option><option value="arrays">Arrays</option><option value="strings">Strings</option><option value="linked-list">Linked List</option><option value="binary-search">Binary Search</option></select><select value={filters.difficulty} onChange={event => onChange({ difficulty: event.target.value })}><option value="all">All difficulty</option><option>Easy</option><option>Medium</option><option>Hard</option></select><select value={filters.status} onChange={event => onChange({ status: event.target.value })}><option value="all">All status</option><option value="solved">Solved</option><option value="unsolved">Unsolved</option></select></div>; }
