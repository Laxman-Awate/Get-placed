import React from 'react';
import { Link } from '../../context/RouteContext';
export function MockTestCard({test}){return <article className="mock-test-card"><div className="mock-card-top"><span className="mock-icon">◉</span><span className={test.isFree?'free-badge':'premium-label'}>{test.isFree?'FREE':'PREMIUM'}</span></div><h2>{test.title}</h2><p>{test.type==='company'?'Company-oriented practice test':'General placement practice test'} · {test.difficulty}</p><div className="mock-meta"><span>{test.totalQuestions} Questions</span><span>{test.durationMinutes} Minutes</span></div><div className="mock-sections">{test.sections.map(section=><span key={section}>{section}</span>)}</div><Link className={test.isFree?'button':'outline-button'} href={test.isFree?`/mock-tests/${test.id}`:'/pricing'}>{test.isFree?'Start test':'Unlock premium'} <span>↗</span></Link></article>;}
