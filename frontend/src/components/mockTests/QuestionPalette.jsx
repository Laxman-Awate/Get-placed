import React from 'react'; export function QuestionPalette({total,current,answers,marked,onSelect}){return <div className="question-palette"><h3>Questions</h3><div>{Array.from({length:total},(_,i)=><button key={i} className={`${i===current?'current ':''}${answers[i]!==undefined?'answered ':''}${marked.includes(i)?'marked':''}`} onClick={()=>onSelect(i)}>{i+1}</button>)}</div><small>Answered · Marked · Current</small></div>;}
