import React from 'react'; export function SubmitTestModal({answers,marked,total,onClose,onSubmit}){return <div className="modal-backdrop"><div className="submit-modal"><h2>Submit test?</h2><p>Review your current progress before submitting.</p><div className="submit-counts"><span><b>{Object.keys(answers).length}</b> answered</span><span><b>{marked.length}</b> marked</span><span><b>{total-Object.keys(answers).length}</b> unanswered</span></div><div><button className="outline-button" onClick={onClose}>Continue test</button><button className="button" onClick={onSubmit}>Submit test</button></div></div></div>;}
