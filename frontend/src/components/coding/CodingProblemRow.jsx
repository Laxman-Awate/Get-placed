import React from 'react'; import { BookmarkButton } from '../practice/BookmarkButton';
import { Link } from '../../context/RouteContext';
export function CodingProblemRow({problem,onSolved,onBookmark}){return <article className="coding-problem-row"><span className={problem.solved?'coding-status solved':'coding-status'}>{problem.solved?'✓':'○'}</span><span className="coding-number">#{String(problem.number).padStart(3,'0')}</span><div><Link href={`/coding/problems/${problem.id}`}>{problem.title}</Link><small>{problem.topic} · {problem.pattern}</small></div><span className={`difficulty ${problem.difficulty.toLowerCase()}`}>{problem.difficulty}</span><span className="coding-state">{problem.solved?'Solved':'Unsolved'}</span><BookmarkButton active={problem.bookmarked} onClick={onBookmark}/><Link className="open-problem" href={`/coding/problems/${problem.id}`}>Open ↗</Link></article>}
