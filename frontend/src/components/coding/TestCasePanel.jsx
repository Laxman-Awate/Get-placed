import React,{useState} from 'react';
export function TestCasePanel({examples}){const [active,setActive]=useState(0);const [custom,setCustom]=useState('');return <div className="test-case-panel"><div className="panel-tabs"><button className={active==='custom'?'active':''} onClick={()=>setActive('custom')}>Custom input</button>{examples.map((_,i)=><button className={active===i?'active':''} key={i} onClick={()=>setActive(i)}>Case {i+1}</button>)}</div>{active==='custom'?<><textarea className="custom-input" value={custom} onChange={e=>setCustom(e.target.value)} placeholder="Store custom input for a future execution service..."/><small className="demo-note">Custom input is stored locally for preview only.</small></>:<pre>{examples[active]}</pre>}</div>}
