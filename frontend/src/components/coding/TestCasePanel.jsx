import React,{useState} from 'react';
export function TestCasePanel({examples,customOnly=false}){const [active,setActive]=useState(0);const [custom,setCustom]=useState('');return <div className="test-case-panel">{!customOnly&&<div className="panel-tabs">{examples.map((_,i)=><button className={active===i?'active':''} onClick={()=>setActive(i)} key={i}>Case {i+1}</button>)}</div>}{customOnly?<><textarea className="custom-input" value={custom} onChange={e=>setCustom(e.target.value)} placeholder="Add custom input for a future execution service..." aria-label="Custom test input"/><small className="demo-note">Custom input is stored locally for preview only.</small></>:<pre>{examples[active]}</pre>}</div>}
