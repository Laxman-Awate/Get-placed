import React from 'react'; export function CodeOutputPanel({state,output}){return <div className="code-output"><h3>Output</h3>{state==='idle'&&<p>Run your code to see output.</p>}{state==='running'&&<p>Running demo preview…</p>}{state==='demo'&&<><strong>Frontend Preview</strong><pre>{output}</pre><p>Demo output only — real code execution will be connected later.</p></>}{state==='pending'&&<><strong>Submission Preview</strong><p>{output}</p></>}{state==='error'&&<p className="error-copy">Unable to process request.</p>}</div>}
