import React from 'react'; export function ContinueCodingCard({problem}){return <article className="continue-coding-card"><div><span className="section-kicker">CONTINUE SOLVING</span><h2>{problem?.title||'Start solving your first problem.'}</h2><p>{problem?`${problem.topic} · ${problem.difficulty} · In progress`:'Browse the problem list to begin building momentum.'}</p></div><Link className="button" href={problem?`/coding/problems/${problem.id}`:'/coding/problems'}>{problem?'Continue':'Browse problems'} <span>↗</span></Link></article>}
import { Link } from '../../context/RouteContext';
