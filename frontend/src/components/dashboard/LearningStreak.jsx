import React from 'react';
import { useLearningStreak } from '../../hooks/useLearningStreak';

const level = count => count === 0 ? 0 : count === 1 ? 1 : count === 2 ? 2 : count <= 4 ? 3 : 4;
export function LearningStreak() { const { weeks, months } = useLearningStreak(); return <section className="streak-panel dashboard-panel"><div className="panel-heading"><div><h2>Learning activity</h2><p>Your consistency is your superpower.</p></div><span className="year-label">Rolling 12 months⌄</span></div><div className="heatmap-wrap"><div className="learning-calendar-scroll"><div className="heatmap-months"><span />{months.map(month => <span key={month.label + month.week} style={{ gridColumn: `${month.week + 2} / span 4` }}>{month.label}</span>)}</div><div className="learning-calendar-body"><div className="learning-weekdays"><span>Mon</span><span>Wed</span><span>Fri</span></div><div className="heatmap-grid" aria-label="Learning activity calendar">{weeks.map((week,index)=><div className="heatmap-week" key={index}>{week.map(cell=><span tabIndex="0" key={cell.id} className={`activity-cell level-${level(cell.activities)} ${cell.inRange?'':'outside'}`} title={`${cell.date.toLocaleDateString()} — ${cell.activities} learning ${cell.activities===1?'activity':'activities'}`} aria-label={`${cell.date.toLocaleDateString()} — ${cell.activities} learning activities`} />)}</div>)}</div></div></div></div><div className="heatmap-legend"><span>Less</span><i className="level-0" /><i className="level-1" /><i className="level-2" /><i className="level-3" /><i className="level-4" /><span>More</span></div></section>; }
