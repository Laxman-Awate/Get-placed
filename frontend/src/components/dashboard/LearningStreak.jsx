import React from 'react';
import { useLearningStreak } from '../../hooks/useLearningStreak';
const months = ['Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct'];
export function LearningStreak() { const cells = useLearningStreak(); return <section className="streak-panel dashboard-panel"><div className="panel-heading"><div><h2>Learning activity</h2><p>Your consistency is your superpower.</p></div><span className="year-label">2026⌄</span></div><div className="heatmap-wrap"><div className="heatmap-months">{months.map(month => <span key={month}>{month}</span>)}</div><div className="heatmap-grid" role="img" aria-label="Learning activity heatmap">{cells.map(cell => <span key={cell.id} className={`activity-cell level-${cell.level}`} title={`${cell.activities} ${cell.activities === 1 ? 'activity' : 'activities'} this day`} />)}</div></div><div className="heatmap-legend"><span>Less</span><i className="level-0" /><i className="level-1" /><i className="level-2" /><i className="level-3" /><i className="level-4" /><span>More</span></div></section>; }
