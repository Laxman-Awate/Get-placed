import React from 'react';
import { useTheme } from '../../context/ThemeContext';

export function SettingsPage() { const { theme, setTheme } = useTheme(); return <div className="student-page"><div className="page-title"><span className="section-kicker">PREFERENCES</span><h1>Settings</h1><p>Manage your account and notification preferences.</p></div><div className="settings-card dashboard-panel"><section><h2>Account</h2><div className="setting-row"><div><strong>Email address</strong><small>arjun@example.com</small></div><button className="subtle-button">Change</button></div><div className="setting-row"><div><strong>Password</strong><small>Last changed recently</small></div><button className="subtle-button">Update</button></div></section><section><h2>Preferences</h2><div className="setting-row"><div><strong>Email notifications</strong><small>Receive updates about your progress</small></div><label className="toggle"><input type="checkbox" defaultChecked /><span /></label></div><div className="setting-row appearance-setting"><div><strong>Appearance</strong><small>Choose the theme for your student workspace</small></div><div className="theme-options" role="group" aria-label="Appearance theme"><button className={theme === 'dark' ? 'theme-option selected' : 'theme-option'} onClick={() => setTheme('dark')} aria-pressed={theme === 'dark'}>◐ Dark</button><button className={theme === 'light' ? 'theme-option selected' : 'theme-option'} onClick={() => setTheme('light')} aria-pressed={theme === 'light'}>☼ Light</button></div></div></section><section className="danger-zone"><h2>Account actions</h2><button className="logout-action" onClick={() => { window.location.href = '/'; }}>Log out of PlacePro</button></section></div></div>; }
