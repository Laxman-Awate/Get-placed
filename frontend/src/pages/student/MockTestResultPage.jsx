import React,{useEffect,useState} from 'react'; import {mockTestService} from '../../services/mockTestService'; import {SectionScoreCard} from '../../components/mockTests/SectionScoreCard';
export function MockTestResultPage(){const id=window.location.pathname.split('/')[2];const [test,setTest]=useState(null);useEffect(()=>{mockTestService.getMockTestById(id).then(setTest)},[id]);if(!test)return <div className="loading-panel">Loading result…</div>;const answers={q1:2,q2:2,q3:0,q4:1,q5:1,q6:2};const correct=test.questions.filter(q=>answers[q.id]===q.correctAnswer).length;const attempted=Object.keys(answers).length;const percent=Math.round(correct/test.questions.length*100);const label=percent>=90?'Excellent':percent>=75?'Good Performance':percent>=60?'Needs Improvement':'Keep Practicing';return <div className="result-page"><div className="result-hero"><span className="section-kicker">TEST COMPLETED</span><h1>{test.title}</h1><div className="result-score"><strong>{percent}%</strong><span>{label}</span></div></div><div className="result-stats">{[['Questions',test.questions.length],['Attempted',attempted],['Correct',correct],['Incorrect',attempted-correct],['Unanswered',test.questions.length-attempted],['Accuracy',`${Math.round(correct/attempted*100)}%`]].map(([label,value])=><div key={label}><small>{label}</small><strong>{value}</strong></div>)}</div><section className="result-section"><h2>Section performance</h2><div className="section-score-grid">{test.sections.map((section,i)=><SectionScoreCard key={section} name={section} score={i===1?2:1} total={2}/>)}</div></section><div className="result-actions"><a className="button" href={`/mock-tests/${id}/review`}>Review answers <span>↗</span></a><a className="outline-button" href={`/mock-tests/${id}/start`}>Retry test</a><a className="subtle-button" href="/mock-tests">Back to mock tests</a></div></div>}
