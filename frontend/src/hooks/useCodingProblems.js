import { useEffect,useMemo,useState } from 'react'; import { codingService } from '../services/codingService';
export function useCodingProblems(){const [problems,setProblems]=useState([]);useEffect(()=>{codingService.getCodingProblems().then(setProblems)},[]);const filter=(values)=>setFilters(current=>({...current,...values}));const [filters,setFilters]=useState({search:'',difficulty:'all',status:'all',topic:'all',pattern:'all'});const visible=useMemo(()=>problems.filter(p=>(!filters.search||`${p.title} ${p.topic} ${p.pattern}`.toLowerCase().includes(filters.search.toLowerCase()))&&(filters.difficulty==='all'||p.difficulty===filters.difficulty)&&(filters.status==='all'||filters.status==='solved'&&p.solved||filters.status==='unsolved'&&!p.solved)&&(filters.topic==='all'||p.topic===filters.topic)&&(filters.pattern==='all'||p.pattern===filters.pattern)),[problems,filters]);return {problems,visible,filters,filter,setProblems};}
